<Window x:Class="PedidoApp.Views.ProdutoModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Produto" Height="250" Width="400">
    <StackPanel Margin="20">
        <TextBlock Text="Nome:"/>
        <TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <TextBlock Text="Código:" Margin="0,10,0,0"/>
        <TextBox Text="{Binding Codigo, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <TextBlock Text="Valor:" Margin="0,10,0,0"/>
        <TextBox Text="{Binding Valor, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Salvar" Command="{Binding SalvarCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Cancelar" Command="{Binding CancelarCommand}" Width="80"/>
        </StackPanel>
    </StackPanel>
</Window>
