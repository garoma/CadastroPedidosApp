<Window x:Class="PedidoApp.Views.PessoaModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pessoa" Height="250" Width="400">
    <StackPanel Margin="20">
        <TextBlock Text="Nome:"/>
        <TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <TextBlock Text="CPF:" Margin="0,10,0,0"/>
        <TextBox Text="{Binding CPF, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" />

        <TextBlock Text="Endereço:" Margin="0,10,0,0"/>
        <TextBox Text="{Binding Endereco, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Salvar" Command="{Binding SalvarCommand}" Width="80" Margin="0,0,10,0"/>
            <Button Content="Cancelar" Command="{Binding CancelarCommand}" Width="80"/>
        </StackPanel>
    </StackPanel>
</Window>
